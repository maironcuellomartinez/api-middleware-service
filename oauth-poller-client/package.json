{
  "name": "oauth-poller-client",
  "version": "1.0.0",
  "private": true,
  "description": "Cliente OAuth2 (client_credentials) que consulta /v1/requests de api-middleware-service en polling.",
  "scripts": {
    "start": "ts-node --transpile-only index.ts",
    "test": "node --require ts-node/register/transpile-only --test index.test.ts"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.3"
  }
}
